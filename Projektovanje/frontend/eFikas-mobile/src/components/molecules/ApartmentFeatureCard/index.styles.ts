import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    card: {
        borderRadius: 10,
        padding: 12,
        marginVertical: 6,
    },
    hstack: {
        justifyContent: "space-between",
        alignItems: "center",
    },
    rightElement: {
        // možeš dodati marginu ili padding ako treba
    },
});

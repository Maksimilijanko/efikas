import { Stack } from "expo-router";
import { Text } from "react-native";

export default function AuthRootLayout() {
    return(
        <Stack>
            <Text>Auth root layout stranica</Text>
        </Stack>
    );
}